{
  "name": "koco-conceptual-image-picker",
  "version": "1.3.2",
  "homepage": "https://github.com/cbcrc/koco-conceptual-image-picker",
  "authors": [
    "David D'Amico <david.damico@cbc.ca>"
  ],
  "description": "Component for launching and receiving the output of a UI for choosing a conceptual image.",
  "main": "src/conceptual-image-picker-ui.js",
  "keywords": [
    "koco"
  ],
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
  },
  "license": "MIT",
  "peerDependencies": {
    "koco": "^1.0.0"
  },
  "dependencies": {
    "koco-dialoger": "^1.1.0",
    "koco-disposer": "^1.1.0",
    "koco-mapping-utilities": "^1.1.0",
    "koco-signal-emitter": "^1.1.0",
    "koco-url-utilities": "^1.1.0",
    "i18next": "^1.9.0",
    "jquery": "^2.1.4",
    "knockout": "^3.4.0"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
